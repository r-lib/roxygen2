#' @include roxygen.R
#' @include roclet.R
roclet()

make.callgraph.roclet <- function(outfile='',
                                  verbose=TRUE) {
  ## Need to abstract some parse.name stuff out of Rd
}
